<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.min.js"></script>
    <title>Pocetna</title>
</head>
<body>
    <div class="container mt-3">
        <a href="/blog/create" class="btn btn-success">Dodaj blog</a>
        
        <% if(blogs.length>0){
            blogs.forEach((e)=>{%>
                <div class="card col-md-12 p-3 mt-3">
                    <h1 class="card-title"><%=e.title%></h1>
                    <p class="card-subtitle text-muted mb-2"><i class="bi bi-clock"></i> <%=e.date.toLocaleDateString().slice(0,-1)%> <%=e.date.toLocaleTimeString().slice(0,-3)%></p>
                    
                    <div class="m3">
                        <a href="/blog/<%=e._id %>" class="btn btn-primary">Procitaj vise</a>
                    </div>
                </div>
            <%})
            }else{%>
                <h2 class="text-danger text-center">Ne postoji ni jedan blog</h2>
            <%}
        %>
    </div>
</body>
</html>